import { SVGProps } from 'react';
import { BehaviorismIcon, GestaltIcon, TSCIcon } from './RealmIcons';
import CloseIcon from './CloseIcon';
import IdeaIcon from './IdeiaIcon';

export type IconProps = SVGProps<SVGSVGElement>;

export { TSCIcon, GestaltIcon, BehaviorismIcon, CloseIcon, IdeaIcon };
